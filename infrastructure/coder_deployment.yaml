AWSTemplateFormatVersion: '2010-09-09'
Description: Cloudformation template for Coder and AWS Modernization Workshop
Parameters:
  KubernetesVersion:
    Description: Kubernetes version
    Type: String
    Default: "1.35"

  CoderVersion:
    Description: Coder version
    Type: String
    Default: "2.36.0"

  EKSClusterName:
    Description: Name of EKS Cluster
    Type: String
    Default: "coder-aws-cluster"

  WorkerNodeInstanceType:
    Description: Worker Node cluster instances
    Type: String
    Default: "t3.large"

  Username:
    Type: String
    Description: Master username for the database
    Default: coder
  
  Password:
    Type: String
    Description: Master password for the database
    NoEcho: true
    MinLength: 8
    Default: coderworkshop
  
  DatabaseName:
    Type: String
    Description: Initial database name
    Default: coder  

  CoderAdminEmail:
    Type: String
    Description: Coder Administrator Email
    Default: admin@example.com
    
  CoderAdminUser:
    Type: String
    Description: Coder Administrator User Name
    Default: admin

  CoderAdminPassword:
    Type: String
    Description: Administrator password for Coder (auto-generated, ignore this parameter)
    NoEcho: true
    Default: "auto-generated"

  CoderAdminName:
    Type: String
    Description: Coder Administrator Full Name
    Default: "Admin User"


  CoderLicenseKey:
    Type: String
    Description: Coder Premium Trial License Key
    Default: ""

  CoderGitOpsTemplateRepoURL:
    Type: String
    Description: Coder GitOps Template Repository URL
    Default: "https://github.com/coder/aws-coder-ai-dlc-gitops.git"
    
  RetryFlag:
    Type: String
    Description: Rerun Coder Build With Existent EKS Deployment (coder is deployed successfully, including CloudFront)
    Default: False

Resources:
################## LOGGING BUCKETS #################
  CloudFrontLoggingBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${AWS::StackName}-cf-logs-${AWS::AccountId}'
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerPreferred
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldLogs
            Status: Enabled
            ExpirationInDays: 90

################## CLOUDFRONT DISTRIBUTION #################
  CoderCloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    DependsOn: CloudFrontLoggingBucket
    Properties:
      DistributionConfig:
        Comment: "Coder Workshop CloudFront Distribution"
        Enabled: true
        PriceClass: PriceClass_100
        Logging:
          Bucket: !GetAtt CloudFrontLoggingBucket.DomainName
          IncludeCookies: false
          Prefix: "cloudfront-logs/"
        Origins:
          - Id: coder-alb-origin
            DomainName: "placeholder.example.com"
            CustomOriginConfig:
              HTTPPort: 80
              HTTPSPort: 443
              OriginProtocolPolicy: http-only
              OriginReadTimeout: 120
              OriginKeepaliveTimeout: 30
        DefaultCacheBehavior:
          TargetOriginId: coder-alb-origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods:
            - GET
            - HEAD
            - OPTIONS
            - PUT
            - POST
            - PATCH
            - DELETE
          CachedMethods:
            - GET
            - HEAD
          ForwardedValues:
            QueryString: true
            Cookies:
              Forward: all
            Headers:
              - "*"
          MinTTL: 0
          DefaultTTL: 0
          MaxTTL: 0

  LoggingBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${AWS::StackName}-logs-${AWS::AccountId}'
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldLogs
            Status: Enabled
            ExpirationInDays: 90

  LoggingBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref LoggingBucket
      PolicyDocument:
        Statement:
          - Sid: AWSLogDeliveryWrite
            Effect: Allow
            Principal:
              Service: delivery.logs.amazonaws.com
            Action: s3:PutObject
            Resource: !Sub '${LoggingBucket.Arn}/*'
            Condition:
              StringEquals:
                s3:x-amz-acl: bucket-owner-full-control
          - Sid: AWSLogDeliveryAclCheck
            Effect: Allow
            Principal:
              Service: delivery.logs.amazonaws.com
            Action: s3:GetBucketAcl
            Resource: !GetAtt LoggingBucket.Arn
          - Sid: ELBAccessLogs
            Effect: Allow
            Principal:
              AWS: arn:aws:iam::797873946194:root
            Action: s3:PutObject
            Resource: !Sub '${LoggingBucket.Arn}/nlb-logs/*'

################## PERMISSIONS AND ROLES #################
  WorkshopCoderRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${EKSClusterName}-workshop-user'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
                - codebuild.amazonaws.com
                - pods.eks.amazonaws.com
            Action:
              - sts:AssumeRole
              - sts:TagSession
      Path: "/"
      Policies:
        - PolicyName: WorkshopCoderPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  # Coder permissions:
                  ## Deployed to RDS + EKS -> EC2, VPC, EKS, CloudFront
                  ## EKS cluster creation triggers provisioning roles an OpenIDCConnectorProvider management

                  # User is working with: 
                  ## Bedrock, AgentCore, secrets (create\update) for API keys, 
                  ## Lambdas and OpenSearch for knowledge bases and logic of retrieval
                  ## SageMaker for deployment of agents in notebooks
                  ## CFN for knowledge bases creation
                  ## for Lyzr\Arize user need to create correct Cross Account role - its precreated but in a case smth incorrect
                  - bedrock:*
                  - bedrock-agentcore:*
                  - aws-marketplace:ViewSubscriptions
                  - aws-marketplace:Subscribe
                  - s3:*
                  - secretsmanager:*
                  - ssm:*
                  - lambda:*
                  - iam:GetRole
                  - iam:GetRolePolicy
                  - iam:ListRolePolicies
                  - iam:ListAttachedRolePolicies
                  - iam:ListRoles
                  - iam:ListRoleTags
                  - iam:CreateRole
                  - iam:PutRolePolicy
                  - iam:AttachRolePolicy
                  - iam:PassRole
                  - iam:TagRole
                  - iam:UntagRole
                  - iam:UpdateAssumeRolePolicy
                  - iam:CreateInstanceProfile
                  - iam:GetInstanceProfile
                  - iam:ListInstanceProfiles
                  - iam:DeleteInstanceProfile
                  - iam:AddRoleToInstanceProfile
                  - iam:RemoveRoleFromInstanceProfile
                  - iam:CreateServiceLinkedRole
                  - iam:CreateOpenIDConnectProvider
                  - iam:GetOpenIDConnectProvider
                  - iam:ListOpenIDConnectProviders
                  - iam:ListOpenIDConnectProviderTags
                  - iam:TagOpenIDConnectProvider
                  - iam:UntagOpenIDConnectProvider
                  - iam:AddClientIDToOpenIDConnectProvider
                  - iam:UpdateOpenIDConnectProviderThumbprint
                  - iam:CreateUser
                  - iam:GetUser
                  - iam:DeleteUser
                  - iam:TagUser
                  - iam:CreateServiceSpecificCredential
                  - iam:ListServiceSpecificCredentials
                  - iam:DeleteServiceSpecificCredential
                  - iam:UpdateServiceSpecificCredential
                Resource: "*"
              # Deny modification of AWS managed and workshop roles
              - Effect: Deny
                Action:
                  - iam:UpdateRole
                  - iam:DeleteRole
                  - iam:UpdateAssumeRolePolicy
                  - iam:AttachRolePolicy
                  - iam:DetachRolePolicy
                  - iam:PutRolePolicy
                  - iam:DeleteRolePolicy
                  - iam:DeleteOpenIDConnectProvider
                  - iam:RemoveClientIDFromOpenIDConnectProvider
                Resource:
                  - !Sub "arn:aws:iam::${AWS::AccountId}:role/WS*"
              - Effect: Allow
                Action:
                  - eks:*
                  - cloudfront:*
                  - ec2:*
                  - aoss:*
                  - dynamodb:*
                  - rds:*
                  - sagemaker:*
                  - logs:*
                  - kms:*
                  - cloudformation:*
                  - ecr:*
                  - codebuild:*
                  - elasticfilesystem:*
                  - events:Describe*
                  - events:List*
                  - sts:AssumeRole
                Resource: "*"

  KMSSecretsKey:
    Type: AWS::KMS::Key
    Properties:
      Description: "key for EKS secrets encryption"
      Enabled: true
      EnableKeyRotation: true
      KeyPolicy:
        Version: '2012-10-17'
        Id: key-default-1
        Statement:
        - Sid: Enable IAM User Permissions
          Effect: Allow
          Principal:
            AWS: !Sub arn:aws:iam::${AWS::AccountId}:root
          Action:
            - kms:Create*
            - kms:Describe*
            - kms:Enable*
            - kms:List*
            - kms:Put*
            - kms:Update*
            - kms:Revoke*
            - kms:Disable*
            - kms:Get*
            - kms:Delete*
            - kms:ScheduleKeyDeletion
            - kms:CancelKeyDeletion
            - kms:GenerateDataKey
            - kms:Encrypt
            - kms:Decrypt
          Resource: '*'

################## INSTANCE PROFILE #####################

  WorkshopInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    DependsOn: WorkshopCoderRole
    Properties:
      Path: "/"
      InstanceProfileName: !Sub '${EKSClusterName}-ec2-workspace-profile'
      Roles:
        - !Ref WorkshopCoderRole

  BedrockApiKeyUser:
    Type: AWS::IAM::User
    DependsOn: WorkshopCoderRole
    Properties:
      UserName: !Sub 'coder-bedrock-api-key-user-${AWS::StackName}'
      Policies:
        - PolicyName: BedrockInvokeAccessPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - bedrock:InvokeModel
                  - bedrock:InvokeModelWithResponseStream
                  - bedrock:Converse
                  - bedrock:ConverseStream
                Resource: '*'
              - Effect: Allow
                Action:
                  - bedrock-mantle:*
                Resource: '*'
      Tags:
        - Key: Purpose
          Value: CoderAIGatewayBedrockMantleKey
        - Key: ManagedBy
          Value: !Ref AWS::StackName

  BedrockMantleApiKeySecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub 'coder-bedrock-mantle-api-key-${AWS::StackName}'
      Description: !Sub >-
        Bedrock Mantle ServiceCredentialSecret for Coder AI Gateway openai-compat provider.
        IAM user: coder-bedrock-api-key-user-${AWS::StackName}.
        Populated by CodeBuild post_build.
      SecretString: 'PENDING_CODEBUILD_POPULATION'
      Tags:
        - Key: Purpose
          Value: CoderAIGatewayBedrockMantleKey
        - Key: ManagedBy
          Value: !Ref AWS::StackName

  FargatePodExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${EKSClusterName}-fargate-pod-execution'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: eks-fargate-pods.amazonaws.com
            Action:
              - sts:AssumeRole
            Condition:
              ArnLike:
                aws:SourceArn: !Sub 'arn:aws:eks:${AWS::Region}:${AWS::AccountId}:fargateprofile/${EKSClusterName}/*'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonEKSFargatePodExecutionRolePolicy
      Tags:
        - Key: Name
          Value: !Sub '${EKSClusterName}-fargate-pod-execution'

################## VPC Setup #####################

  CoderVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 192.168.0.0/16
      EnableDnsHostnames: true
      EnableDnsSupport: true
      Tags:
        - Key: Name
          Value: !Sub '${EKSClusterName}/VPC'
        - Key: alpha.eksctl.io/cluster-name
          Value: ${EKSClusterName}

  InternetGateway:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/InternetGateway

  InternetGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      InternetGatewayId: !Ref InternetGateway
      VpcId: !Ref CoderVPC

  PublicSubnet1:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [0, !GetAZs '']
      CidrBlock: 192.168.0.0/19
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetPublic1
        - Key: kubernetes.io/role/elb
          Value: '1'

  PublicSubnet2:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [1, !GetAZs '']
      CidrBlock: 192.168.32.0/19
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetPublic2
        - Key: kubernetes.io/role/elb
          Value: '1'

  PrivateSubnet1:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [0, !GetAZs '']
      CidrBlock: 192.168.96.0/19
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetPrivate1
        - Key: kubernetes.io/role/internal-elb
          Value: '1'

  PrivateSubnet2:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [1, !GetAZs '']
      CidrBlock: 192.168.128.0/19
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetPrivate2
        - Key: kubernetes.io/role/internal-elb
          Value: '1'

  FargateSubnet1:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [0, !GetAZs '']
      CidrBlock: 192.168.160.0/19
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetFargate1
        - Key: kubernetes.io/role/internal-elb
          Value: '1'

  FargateSubnet2:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref CoderVPC
      AvailabilityZone: !Select [1, !GetAZs '']
      CidrBlock: 192.168.192.0/19
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/SubnetFargate2
        - Key: kubernetes.io/role/internal-elb
          Value: '1'

  NatGateway1EIP:
    Type: AWS::EC2::EIP
    DependsOn: InternetGatewayAttachment
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value:  ${EKSClusterName}/NATIP

  NatGateway2EIP:
    Type: AWS::EC2::EIP
    DependsOn: InternetGatewayAttachment
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value:  ${EKSClusterName}/NATIP

  NatGateway1:
    Type: AWS::EC2::NatGateway
    Properties:
      AllocationId: !GetAtt NatGateway1EIP.AllocationId
      SubnetId: !Ref PublicSubnet1
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/NATGateway

  NatGateway2:
    Type: AWS::EC2::NatGateway
    Properties:
      AllocationId: !GetAtt NatGateway2EIP.AllocationId
      SubnetId: !Ref PublicSubnet2
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/NATGateway

  PublicRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref CoderVPC
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/PublicRouteTable

  PrivateRouteTable1:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref CoderVPC
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/PrivateRouteTable1

  PrivateRouteTable2:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref CoderVPC
      Tags:
        - Key: Name
          Value: ${EKSClusterName}/PrivateRouteTable2

  PublicRoute:
    Type: AWS::EC2::Route
    DependsOn: InternetGatewayAttachment
    Properties:
      RouteTableId: !Ref PublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref InternetGateway

  PrivateRoute1:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PrivateRouteTable1
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway1

  PrivateRoute2:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PrivateRouteTable2
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway2

  PublicSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PublicRouteTable
      SubnetId: !Ref PublicSubnet1

  PublicSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PublicRouteTable
      SubnetId: !Ref PublicSubnet2

  PrivateSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PrivateRouteTable1
      SubnetId: !Ref PrivateSubnet1

  PrivateSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PrivateRouteTable2
      SubnetId: !Ref PrivateSubnet2

  FargateSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PrivateRouteTable1
      SubnetId: !Ref FargateSubnet1

  FargateSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PrivateRouteTable2
      SubnetId: !Ref FargateSubnet2

################## EFS for Fargate Workspaces #####################

  EfsSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow NFS from VPC for Coder workspace EFS
      VpcId: !Ref CoderVPC
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 2049
          ToPort: 2049
          CidrIp: 192.168.0.0/16
          Description: Allow NFS from VPC
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: !Sub '${EKSClusterName}-efs-sg'

  EfsFileSystem:
    Type: AWS::EFS::FileSystem
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Encrypted: true
      ThroughputMode: elastic
      FileSystemTags:
        - Key: Name
          Value: !Sub '${EKSClusterName}-workspace-homes'

  EfsMountTarget1:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref EfsFileSystem
      SubnetId: !Ref FargateSubnet1
      SecurityGroups:
        - !Ref EfsSecurityGroup

  EfsMountTarget2:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref EfsFileSystem
      SubnetId: !Ref FargateSubnet2
      SecurityGroups:
        - !Ref EfsSecurityGroup

################## Secrets Manager #####################

  CoderAdminPasswordSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${EKSClusterName}-admin-password'
      Description: Coder Administrator Password
      GenerateSecretString:
        PasswordLength: 12
        ExcludeCharacters: '"@/\\'
        ExcludePunctuation: false
        IncludeSpace: false
        RequireEachIncludedType: true

  CoderSessionTokenSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${EKSClusterName}-session-token'
      Description: Coder Session Token for API access

################## Aurora PostgreSQL #####################

  AuroraSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security group for Aurora PostgreSQL cluster
      VpcId: !Ref CoderVPC
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          CidrIp: 192.168.0.0/16
          Description: Allow PostgreSQL access from VPC
      SecurityGroupEgress:
        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: 0.0.0.0/0
          Description: Allow HTTPS for AWS API calls
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          CidrIp: 192.168.0.0/16
          Description: Allow PostgreSQL within VPC
      Tags:
        - Key: Name
          Value: !Sub '${EKSClusterName}-aurora-sg'

  CoderAuroraSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupName: !Sub '${EKSClusterName}-aurora-subnet-group'
      DBSubnetGroupDescription: "Subnet group for Coder' Aurora PostgreSQL - same as EKS cluster"
      SubnetIds: 
        - !Ref PrivateSubnet1
        - !Ref PrivateSubnet2

  # Create Aurora PostgreSQL Serverless cluster
  CoderAuroraCluster:
    Type: AWS::RDS::DBCluster
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      DBClusterIdentifier: !Sub '${EKSClusterName}-aurora'
      Engine: aurora-postgresql
      EngineVersion: '16.11'
      MasterUsername: !Ref Username
      MasterUserPassword: !Ref Password
      DatabaseName: !Ref DatabaseName
      Port: 5432
      DBSubnetGroupName: !Ref CoderAuroraSubnetGroup
      VpcSecurityGroupIds:
        - !Ref AuroraSecurityGroup
      BackupRetentionPeriod: 3
      StorageEncrypted: true
      ServerlessV2ScalingConfiguration:
        MinCapacity: 0.5
        MaxCapacity: 128.0

  # Create Aurora PostgreSQL instance
  CoderAuroraInstance:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      DBInstanceIdentifier: !Sub '${EKSClusterName}-aurora-instance'
      DBInstanceClass: db.serverless
      Engine: aurora-postgresql
      DBClusterIdentifier: !Ref CoderAuroraCluster
      PubliclyAccessible: false

################## EKS Bootstrap #####################

  BuildProject:
    DependsOn: [WorkshopInstanceProfile, CoderAuroraInstance, FargatePodExecutionRole, EfsMountTarget1, EfsMountTarget2]
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub CodeBuild-${AWS::StackName}
      ServiceRole: !GetAtt WorkshopCoderRole.Arn
      Artifacts:
        Type: NO_ARTIFACTS
      LogsConfig:
        CloudWatchLogs:
          Status: ENABLED
          GroupName: !Sub "/aws/codebuild/CodeBuild-${AWS::StackName}"
          StreamName: build-log
      EncryptionKey: !GetAtt KMSSecretsKey.Arn
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: public.ecr.aws/v1u7c9b4/coder-codebuild:latest
        EnvironmentVariables:
          - Name: WAIT_HANDLE_URL
            Value: !Ref EKSClusterWaitHandle
          - Name: KMS_ARN
            Value: !GetAtt KMSSecretsKey.Arn
          - Name: EKSClusterName
            Value: !Ref EKSClusterName
          - Name: AWSRegion
            Value: !Sub "${AWS::Region}"
          - Name: STACK_NAME
            Value: !Ref AWS::StackName
          - Name: KubernetesVersion
            Value: !Ref KubernetesVersion
          - Name: CoderVersion
            Value: !Ref CoderVersion
          - Name: WorkerNodeInstanceType
            Value: !Ref WorkerNodeInstanceType
          - Name: VpcId
            Value: !Ref CoderVPC
          - Name: PublicSubnet1
            Value: !Ref PublicSubnet1
          - Name: PublicSubnet2
            Value: !Ref PublicSubnet2
          - Name: PrivateSubnet1
            Value: !Ref PrivateSubnet1
          - Name: PrivateSubnet2
            Value: !Ref PrivateSubnet2
          - Name: Username
            Value: !Ref Username
          - Name: Password
            Value: !Ref Password
          - Name: DatabaseName
            Value: !Ref DatabaseName
          - Name: PostgreSQLClusterAddress
            Value: !GetAtt CoderAuroraCluster.Endpoint.Address
          - Name: PostgreSQLPort
            Value: !GetAtt CoderAuroraCluster.Endpoint.Port
          - Name: CoderAdminEmail
            Value: !Ref CoderAdminEmail
          - Name: CoderAdminName
            Value: !Ref CoderAdminName
          - Name: CoderAdminPassword
            Value: !Sub '{{resolve:secretsmanager:${CoderAdminPasswordSecret}:SecretString}}'
          - Name: CoderAdminUser
            Value: !Ref CoderAdminUser
          - Name: CoderLicenseKey
            Value: !Ref CoderLicenseKey      
          - Name: RetryFlag
            Value: !Ref RetryFlag    
          - Name: CoderGitOpsTemplateRepoURL
            Value: !Ref CoderGitOpsTemplateRepoURL  
          - Name: CoderSessionTokenSecretArn
            Value: !Ref CoderSessionTokenSecret  
          - Name: WorkshopRoleName
            Value: !Ref WorkshopCoderRole
          - Name: CLOUDFRONT_DISTRIBUTION_ID
            Value: !Ref CoderCloudFrontDistribution
          - Name: CLOUDFRONT_DOMAIN
            Value: !GetAtt CoderCloudFrontDistribution.DomainName
          - Name: FargateSubnet1
            Value: !Ref FargateSubnet1
          - Name: FargateSubnet2
            Value: !Ref FargateSubnet2
          - Name: FargatePodExecutionRoleArn
            Value: !GetAtt FargatePodExecutionRole.Arn
          - Name: EfsFileSystemId
            Value: !Ref EfsFileSystem
          - Name: ClaudeCodeImageUri
            Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-claude-code:latest'
          - Name: KiroCliImageUri
            Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-kiro-cli:latest'
          - Name: ChallengeImageUri
            Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-challenge:latest'
          - Name: BEDROCK_MANTLE_SECRET_ARN
            Value: !Ref BedrockMantleApiKeySecret
          
      Source:
        Type: NO_SOURCE
        BuildSpec: |
          version: 0.2
          phases:
            install:
              runtime-versions:
                python: 3.12
              commands:
                - echo ">>> installed python 3.12"
                - export BUILD_SUCCESS=false
            pre_build:
              commands:
                - echo ">>> build cluster config with Region $AWSRegion"
                - |
                  cat << EOF > cluster-config.yaml
                  apiVersion: eksctl.io/v1alpha5
                  kind: ClusterConfig
                  metadata:
                    name: ${EKSClusterName}
                    region: ${AWSRegion}
                    version: "${KubernetesVersion}"
                  
                  autoModeConfig:
                    enabled: true
                  
                  vpc:
                    id: ${VpcId}
                    subnets:
                      public:
                        us-west-2a:
                          id: ${PublicSubnet1}
                        us-west-2b:
                          id: ${PublicSubnet2}
                      private:
                        us-west-2a:
                          id: ${PrivateSubnet1}
                        us-west-2b:
                          id: ${PrivateSubnet2}
                  
                  addons:
                    - name: aws-ebs-csi-driver
                      version: latest
                    - name: aws-efs-csi-driver
                      version: latest  
                      
                  cloudWatch:
                    clusterLogging:
                      enableTypes: ["*"]
                  
                  iam:
                    withOIDC: true
                  
                  secretsEncryption:
                    keyARN: ${KMS_ARN}
                  EOF
                - cat cluster-config.yaml
            build:
              commands:
                - echo ">>> creating EKS cluster in region $AWSRegion with Auto Mode"
                - |
                  if aws eks describe-cluster --name "${EKSClusterName}" --region "${AWSRegion}" >/dev/null 2>&1; then
                    echo "EKS cluster ${EKSClusterName} already exists; skipping creation (idempotent re-run)."
                    export BUILD_SUCCESS=true
                  elif [ "$RetryFlag" != "True" ]; then
                    if eksctl create cluster -f cluster-config.yaml; then
                      export BUILD_SUCCESS=true
                      echo "EKS cluster creation successful"
                    else
                      echo "EKS cluster creation failed"
                      if [ -n "$WAIT_HANDLE_URL" ]; then
                        curl -X PUT -H 'Content-Type:' --data-binary '{"Status":"FAILURE","Reason":"EKS Cluster Creation Failed","UniqueId":"EKSCluster","Data":"EKS Cluster Creation Failed"}' "$WAIT_HANDLE_URL"
                        echo "Sent FAILURE signal to CloudFormation"
                      fi
                      exit 1
                    fi
                  else
                    export BUILD_SUCCESS=true
                  fi
            post_build:
              commands:
                - echo ">>> Creating post-build script"
                - |
                  cat > post_build.sh << 'POST_EOF'
                  #!/bin/bash
                  set -e

                  # Signal CloudFormation FAILURE immediately on any error so the
                  # WaitCondition fails fast instead of waiting out its 45-min timeout.
                  signal_failure() {
                    [ -n "$WAIT_HANDLE_URL" ] || return 0
                    curl -sf -X PUT -H "Content-Type:" --data-binary "{\"Status\":\"FAILURE\",\"Reason\":\"CodeBuild post_build failed\",\"UniqueId\":\"EKSCluster\",\"Data\":\"post_build failed\"}" "$WAIT_HANDLE_URL" || true
                  }
                  trap 'rc=$?; if [ "$rc" -ne 0 ]; then signal_failure; fi' EXIT
                  
                  if [ "$BUILD_SUCCESS" = "true" ]; then
                    echo "EKS cluster creation successful"
                    
                    aws eks update-kubeconfig --region ${AWSRegion} --name ${EKSClusterName}
                    
                    aws eks create-addon --cluster-name ${EKSClusterName} --addon-name coredns --region ${AWSRegion} || true
                    echo "Waiting for CoreDNS addon to become ACTIVE..."
                    aws eks wait addon-active --cluster-name ${EKSClusterName} --addon-name coredns --region ${AWSRegion} || true
                    
                    ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
                    PARTICIPANT_ROLE_ARN="arn:aws:iam::$ACCOUNT_ID:role/WSParticipantRole"
                    
                    git clone $CoderGitOpsTemplateRepoURL
                    CODER_GITOPS_DIR=$(basename "$CoderGitOpsTemplateRepoURL" .git)
                    # Absolute path to the clone — later steps cd into subdirs (e.g.
                    # templates/), so relative "$CODER_GITOPS_DIR/..." paths break.
                    CODER_GITOPS_ABS="$(cd "$CODER_GITOPS_DIR" && pwd)"

                    cp "$CODER_GITOPS_DIR/infrastructure/k8s/gp3-storageclass.yaml" storageclass.yaml
                    cp "$CODER_GITOPS_DIR/infrastructure/helm/coder-values.yaml" coder-core-values-v2.yaml
                    helm repo add coder-v2 https://helm.coder.com/v2
                    helm repo update

                    if [ "$RetryFlag" != "True" ]; then
                      kubectl apply -f storageclass.yaml
                      
                      kubectl create configmap aws-auth -n kube-system --from-literal=mapRoles="[{\"rolearn\":\"$PARTICIPANT_ROLE_ARN\",\"username\":\"workshop-user\",\"groups\":[\"system:masters\"]}]" --dry-run=client -o yaml | kubectl apply -f -
                      kubectl create namespace coder
                      
                      kubectl create namespace coder-ws
                      
                      kubectl apply -f "$CODER_GITOPS_DIR/infrastructure/k8s/coder-workspace-rbac.yaml"
                      kubectl apply -f "$CODER_GITOPS_DIR/infrastructure/k8s/efs-storageclass.yaml"

                      # EC2 Spot workspace lane, auto-scaled by EKS Auto Mode.
                      kubectl apply -f "$CODER_GITOPS_DIR/infrastructure/k8s/spot-nodepool.yaml"

                      # Fargate lane: only workspace pods labelled compute=fargate are
                      # placed on Fargate; spot-lane pods (compute=spot) are excluded and
                      # scheduled onto the coder-ws-spot NodePool instead.
                      aws eks create-fargate-profile \
                        --cluster-name ${EKSClusterName} \
                        --fargate-profile-name coder-workspaces \
                        --pod-execution-role-arn ${FargatePodExecutionRoleArn} \
                        --subnets ${FargateSubnet1} ${FargateSubnet2} \
                        --selectors "namespace=coder-ws,labels={compute=fargate}"
                      
                      echo "Waiting for Fargate profile to become ACTIVE..."
                      aws eks wait fargate-profile-active \
                        --cluster-name ${EKSClusterName} \
                        --fargate-profile-name coder-workspaces
                      echo "Fargate profile is ACTIVE"

                      PostgreSQLConnectionURL="postgresql://${Username}:${Password}@${PostgreSQLClusterAddress}:${PostgreSQLPort}/${DatabaseName}"
                      kubectl create secret generic coder-db-url -n coder --from-literal=url="$PostgreSQLConnectionURL" --dry-run=client -o yaml | kubectl apply -f -
                 
                      helm install coder coder-v2/coder --namespace coder --values coder-core-values-v2.yaml --version "${CoderVersion}"
                    fi

                    for i in {1..30}; do
                      kubectl get service coder -n coder -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' > lb_hostname.txt 2>/dev/null || echo "" > lb_hostname.txt
                      CODER_LB_HOSTNAME=$(cat lb_hostname.txt | tr -d '\n')
                      if [ -n "$CODER_LB_HOSTNAME" ]; then
                        echo "Load balancer ready: $CODER_LB_HOSTNAME"
                        break
                      fi
                      echo "Waiting for load balancer... ($i/30)"
                      sleep 30
                    done

                    if [ "$RetryFlag" != "True" ]; then
                      aws eks create-pod-identity-association \
                      --cluster-name ${EKSClusterName} \
                      --namespace coder \
                      --service-account coder \
                      --role-arn arn:aws:iam::$ACCOUNT_ID:role/${WorkshopRoleName}

                      echo "Configuring IRSA for Fargate workspace pods..."
                      
                      chmod +x "$CODER_GITOPS_DIR/infrastructure/scripts/irsa-trust-policy-update.sh"
                      "$CODER_GITOPS_DIR/infrastructure/scripts/irsa-trust-policy-update.sh" \
                        "${EKSClusterName}" \
                        "${WorkshopRoleName}" \
                        "coder-ws" \
                        "coder-ws"
                      
                      sed "s|PLACEHOLDER_ROLE_ARN|arn:aws:iam::${ACCOUNT_ID}:role/${WorkshopRoleName}|g" "$CODER_GITOPS_DIR/infrastructure/k8s/coder-ws-serviceaccount.yaml" \
                        | kubectl apply -f -
                      echo "Created coder-ws service account with IRSA annotation"
                    fi

                    if [ -n "$CODER_LB_HOSTNAME" ]; then
                      CURRENT_CF_ORIGIN=$(aws cloudfront get-distribution-config --id "$CLOUDFRONT_DISTRIBUTION_ID" --query 'DistributionConfig.Origins.Items[0].DomainName' --output text 2>/dev/null || echo "")
                      if [ "$CURRENT_CF_ORIGIN" = "$CODER_LB_HOSTNAME" ]; then
                        echo "CloudFront origin already points to $CODER_LB_HOSTNAME; skipping distribution update (idempotent re-run)."
                      else
                        echo "Updating CloudFront distribution $CLOUDFRONT_DISTRIBUTION_ID with origin $CODER_LB_HOSTNAME"

                        CF_CONFIG=$(aws cloudfront get-distribution-config --id "$CLOUDFRONT_DISTRIBUTION_ID")
                        ETAG=$(echo "$CF_CONFIG" | jq -r '.ETag')
                        DIST_CONFIG=$(echo "$CF_CONFIG" | jq '.DistributionConfig')

                        UPDATED_CONFIG=$(echo "$DIST_CONFIG" | jq --arg hostname "$CODER_LB_HOSTNAME" \
                          '.Origins.Items[0].DomainName = $hostname')

                        echo "$UPDATED_CONFIG" > updated-cf-config.json

                        aws cloudfront update-distribution \
                          --id "$CLOUDFRONT_DISTRIBUTION_ID" \
                          --distribution-config file://updated-cf-config.json \
                          --if-match "$ETAG"
                      fi

                      DISTRIBUTION_ID="$CLOUDFRONT_DISTRIBUTION_ID"

                      echo "CloudFront Distribution ID: $DISTRIBUTION_ID"
                      echo "CloudFront Domain: $CLOUDFRONT_DOMAIN"

                      echo "Waiting for CloudFront distribution to deploy..."
                      while true; do
                        STATUS=$(aws cloudfront get-distribution --id $DISTRIBUTION_ID --query 'Distribution.Status' --output text)
                        echo "Distribution Status: $STATUS"
                        if [ "$STATUS" = "Deployed" ]; then
                          echo "CloudFront distribution is ready!"
                          break
                        fi
                        echo "Waiting 60 seconds before checking again..."
                        sleep 60
                      done

                      CODER_CLOUDFRONT_URL="https://$CLOUDFRONT_DOMAIN"
                      CODER_CLOUDFRONT_WILDCARD="*.$CLOUDFRONT_DOMAIN"

                      echo "New Coder Access URL: $CODER_CLOUDFRONT_URL"
                      echo "New Coder Wildcard URL: $CODER_CLOUDFRONT_WILDCARD"

                      sed -i "s|https://coder.example.com|$CODER_CLOUDFRONT_URL|g" coder-core-values-v2.yaml
                      sed -i "s|\*.coder.example.com|$CODER_CLOUDFRONT_WILDCARD|g" coder-core-values-v2.yaml
                      helm upgrade coder coder-v2/coder --namespace coder --values coder-core-values-v2.yaml --version "${CoderVersion}"

                      # Ensure coder-db-url matches the current Aurora endpoint. On a RetryFlag=True
                      # re-run the Aurora cluster may have been recreated with a new endpoint; refresh
                      # the secret (idempotent) and restart coder so it reconnects. No-op on a fresh
                      # deploy where the value is already current.
                      PostgreSQLConnectionURL="postgresql://${Username}:${Password}@${PostgreSQLClusterAddress}:${PostgreSQLPort}/${DatabaseName}"
                      if kubectl -n coder get secret coder-db-url >/dev/null 2>&1; then
                        EXISTING_DB_URL=$(kubectl -n coder get secret coder-db-url -o jsonpath='{.data.url}' 2>/dev/null | base64 -d 2>/dev/null || echo "")
                        if [ "$EXISTING_DB_URL" != "$PostgreSQLConnectionURL" ]; then
                          echo "coder-db-url differs from the current Aurora endpoint; refreshing and restarting coder."
                          kubectl -n coder create secret generic coder-db-url --from-literal=url="$PostgreSQLConnectionURL" --dry-run=client -o yaml | kubectl apply -f -
                          kubectl -n coder rollout restart deploy/coder || true
                        fi
                      fi

                      kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=coder -n coder --timeout=300s

                      echo "Cycling CloudFront connection pool..."
                      for i in {1..20}; do
                        curl -s -f "$CODER_CLOUDFRONT_URL/api/v2/buildinfo" > /dev/null 2>&1 || true
                        sleep 3
                      done

                      echo "=== Health Check: Verifying Coder Pod Status ==="
                      kubectl get pods -n coder -o wide
                      POD_STATUS=$(kubectl get pods -n coder -o jsonpath='{.items[0].status.phase}')
                      POD_READY=$(kubectl get pods -n coder -o jsonpath='{.items[0].status.conditions[?(@.type=="Ready")].status}')
                      echo "Pod Status: $POD_STATUS, Ready: $POD_READY"
                      
                      if [ "$POD_STATUS" != "Running" ] || [ "$POD_READY" != "True" ]; then
                        echo "WARNING: Coder pod is not ready. Status: $POD_STATUS, Ready: $POD_READY"
                        kubectl describe pod -n coder $(kubectl get pods -n coder -o jsonpath='{.items[0].metadata.name}')
                      fi

                      echo "=== Health Check: Testing Direct NLB Connectivity ==="
                      NLB_HEALTH_CHECK=$(curl -s -o /dev/null -w "%{http_code}" -m 10 http://$CODER_LB_HOSTNAME/api/v2/buildinfo)
                      echo "NLB Health Check Response: $NLB_HEALTH_CHECK"
                      
                      if [ "$NLB_HEALTH_CHECK" != "200" ]; then
                        echo "WARNING: NLB health check failed with status $NLB_HEALTH_CHECK"
                      else
                        echo "SUCCESS: NLB is responding correctly"
                      fi

                      echo "=== Health Check: Testing CloudFront Connectivity ==="
                      CF_HEALTH_CHECK=$(curl -s -o /dev/null -w "%{http_code}" -m 10 $CODER_CLOUDFRONT_URL/api/v2/buildinfo)
                      echo "CloudFront Health Check Response: $CF_HEALTH_CHECK"
                      
                      if [ "$CF_HEALTH_CHECK" != "200" ]; then
                        echo "WARNING: CloudFront health check failed with status $CF_HEALTH_CHECK"
                        echo "Waiting additional 60 seconds for CloudFront propagation..."
                        sleep 60
                        CF_HEALTH_CHECK=$(curl -s -o /dev/null -w "%{http_code}" -m 10 $CODER_CLOUDFRONT_URL/api/v2/buildinfo)
                        echo "CloudFront Health Check Response (retry): $CF_HEALTH_CHECK"
                      else
                        echo "SUCCESS: CloudFront is responding correctly"
                      fi

                      echo "Deploying Coder GitOps Template Repository: $CoderGitOpsTemplateRepoURL"
                      CODER_AGENT_URL="$CODER_CLOUDFRONT_URL"; export CODER_AGENT_URL
                      cd $CODER_GITOPS_DIR/templates 

                      if [ "$RetryFlag" != "True" ]; then
                        echo "Creating initial Coder Administrator User"
                        LOGIN_SUCCESS=false
                        for attempt in {1..10}; do
                          echo "Login attempt $attempt/10"
                          if coder login $CODER_CLOUDFRONT_URL \
                            --first-user-email $CoderAdminEmail \
                            --first-user-username $CoderAdminUser \
                            --first-user-password "$CoderAdminPassword" \
                            --first-user-full-name "$CoderAdminName"; then
                            LOGIN_SUCCESS=true
                            echo "Coder login successful"
                            break
                          fi
                          echo "Login failed, waiting $((attempt * 15)) seconds before retry..."
                          sleep $((attempt * 15))
                        done

                        CODER_SESSION_TOKEN=$(coder tokens create --lifetime 120)
                        echo "Session token created successfully"

                        aws secretsmanager put-secret-value \
                          --secret-id "$CoderSessionTokenSecretArn" \
                          --secret-string "$CODER_SESSION_TOKEN"
                        echo "Session token stored in Secrets Manager" 

                        if [ -n "$CoderLicenseKey" ]; then
                          coder licenses add -l "$CoderLicenseKey"
                        fi

                        echo "=== Configuring Coder AI Providers ==="
                        CODER_URL="${CODER_CLOUDFRONT_URL}"
                        BEDROCK_REGION="us-east-1"  # Anthropic inference profiles available in us-east-1
                        BEDROCK_ENDPOINT="https://bedrock-runtime.${BEDROCK_REGION}.amazonaws.com"
                        BEDROCK_MANTLE_ENDPOINT="https://bedrock-mantle.${BEDROCK_REGION}.api.aws/v1"
                        BEDROCK_USER="coder-bedrock-api-key-user-${STACK_NAME}"

                        EXISTING_SECRET=$(aws secretsmanager get-secret-value \
                          --secret-id "${BEDROCK_MANTLE_SECRET_ARN}" \
                          --region "${AWSRegion}" \
                          --query SecretString --output text 2>/dev/null || echo "")

                        if [ -z "${EXISTING_SECRET}" ] || [ "${EXISTING_SECRET}" = "PENDING_CODEBUILD_POPULATION" ]; then
                          echo "Creating Bedrock service-specific credential for ${BEDROCK_USER}..."
                          for CRED_ID in $(aws iam list-service-specific-credentials \
                            --user-name "${BEDROCK_USER}" \
                            --service-name bedrock.amazonaws.com \
                            --query 'ServiceSpecificCredentials[*].ServiceSpecificCredentialId' \
                            --output text 2>/dev/null); do
                            aws iam delete-service-specific-credential \
                              --user-name "${BEDROCK_USER}" \
                              --service-specific-credential-id "${CRED_ID}" 2>/dev/null || true
                          done

                          CRED_JSON=$(aws iam create-service-specific-credential \
                            --user-name "${BEDROCK_USER}" \
                            --service-name bedrock.amazonaws.com \
                            --credential-age-days 30)
                          BEDROCK_MANTLE_KEY=$(echo "${CRED_JSON}" | python3 -c \
                            "import json,sys; print(json.load(sys.stdin)['ServiceSpecificCredential']['ServiceCredentialSecret'])")
                          aws secretsmanager put-secret-value \
                            --secret-id "${BEDROCK_MANTLE_SECRET_ARN}" \
                            --secret-string "${BEDROCK_MANTLE_KEY}" \
                            --region "${AWSRegion}"
                          echo "ServiceCredentialSecret stored in Secrets Manager."
                        else
                          echo "Bedrock Mantle secret already populated; reusing."
                          BEDROCK_MANTLE_KEY="${EXISTING_SECRET}"
                        fi

                        echo "=== Configuring Coder AI Providers + Models via Terraform (coderd) ==="
                        # Providers and Coder Agents models are managed declaratively in
                        # $CODER_GITOPS_DIR/ai-providers via the coderd Terraform provider,
                        # replacing the direct /api/v2/ai/providers and
                        # /api/experimental/chats/model-configs API calls.
                        export CODER_AGENT_URL="${CODER_URL}"
                        export BEDROCK_REGION BEDROCK_ENDPOINT BEDROCK_MANTLE_ENDPOINT
                        export BEDROCK_MODEL="global.anthropic.claude-opus-4-6-v1"
                        export BEDROCK_SMALL_FAST_MODEL="global.anthropic.claude-haiku-4-5-20251001-v1:0"
                        export BEDROCK_MANTLE_KEY

                        ( cd "$CODER_GITOPS_ABS/ai-providers" && bash ai_providers_gitops.sh "$CODER_SESSION_TOKEN" )
                        echo "=== AI Provider Configuration Complete ==="
                      else
                        CODER_SESSION_TOKEN=$(aws secretsmanager get-secret-value --secret-id "$CoderSessionTokenSecretArn" --query SecretString --output text)
                        echo "Session token received successfully"
                        
                        echo "Logging in to Coder"
                        LOGIN_SUCCESS=false
                        if coder login $CODER_CLOUDFRONT_URL --use-token-as-session true; then
                          LOGIN_SUCCESS=true
                          echo "Coder login successful"
                        fi
                      fi

                      echo "Deploying GitOps templates via Terraform: $CODER_AGENT_URL"
                      if [ -n "$AWS_CONTAINER_CREDENTIALS_RELATIVE_URI" ]; then
                        export AWS_CONTAINER_CREDENTIALS_RELATIVE_URI
                      fi

                      if [ -n "$AWS_ACCESS_KEY_ID" ]; then
                        export AWS_ACCESS_KEY_ID
                        export AWS_SECRET_ACCESS_KEY
                        export AWS_SESSION_TOKEN
                      fi

                      export AWS_DEFAULT_REGION="$AWSRegion"
                      export AWS_REGION="$AWSRegion"

                      echo "Testing AWS credentials..."
                      aws sts get-caller-identity

                      terraform init

                      sleep 120

                      ./templates_gitops.sh $CODER_SESSION_TOKEN
                    fi
                    
                    # Use CLOUDFRONT_DOMAIN env var directly 
                    SSM_CODER_URL="https://${CLOUDFRONT_DOMAIN}"
                    echo "Setting SSM parameters..."
                    echo "SSM_CODER_URL: ${SSM_CODER_URL}"
                    aws configure set cli_follow_urlparam false
                    aws ssm put-parameter --name "/eks/${EKSClusterName}/cloudfront-url" --value "${SSM_CODER_URL}" --type "String" --overwrite
                    aws ssm put-parameter --name "/eks/${EKSClusterName}/cluster-name" --value "${EKSClusterName}" --type "String" --overwrite
                    aws ssm put-parameter --name "/eks/${EKSClusterName}/region" --value "${AWSRegion}" --type "String" --overwrite
                  fi
                  POST_EOF
                - chmod +x post_build.sh
                - ./post_build.sh
                - echo ">>> Signaling CloudFormation"
                - |
                  if [ "$BUILD_SUCCESS" = "true" ]; then
                    curl -X PUT -H 'Content-Type:' --data-binary '{"Status":"SUCCESS","Reason":"EKS Cluster Created","UniqueId":"EKSCluster","Data":"Deployment Complete"}' "$WAIT_HANDLE_URL"
                  else
                    curl -X PUT -H 'Content-Type:' --data-binary '{"Status":"FAILURE","Reason":"EKS Cluster Creation Failed","UniqueId":"EKSCluster","Data":"Deployment Failed"}' "$WAIT_HANDLE_URL"
                  fi

  TriggerBuildLambdaIamRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
                - codebuild.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/CloudWatchLogsFullAccess
        - arn:aws:iam::aws:policy/AWSCodeBuildAdminAccess
      Policies:
        - PolicyName: !Sub IAMPolicy-${AWS::StackName}
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                    - codebuild:StartBuild
                    - codebuild:BatchGetBuilds
                    - codebuild:ListBuildsForProject
                    - codebuild:BatchGetProjects
                    - codebuild:StopBuild
                Resource: !GetAtt BuildProject.Arn

  TriggerBuildLambda:
    Type: AWS::Lambda::Function
    Properties:
      Description: function to trigger CodeBuild project
      Handler: index.handler
      Role: !GetAtt TriggerBuildLambdaIamRole.Arn
      Runtime: python3.12
      Timeout: 60
      ReservedConcurrentExecutions: 5
      Code:
        ZipFile: |
          import boto3
          import logging
          import json
          import urllib.request
          import time

          logger = logging.getLogger()
          logger.setLevel(logging.INFO)

          codebuild_client = boto3.client('codebuild')

          def handler(event, context):
              logger.info('Incoming Event: {0}'.format(event))
              response = {}
              response['PhysicalResourceId'] = 'codebuildtrigger-' + str(int(time.time()))
              response['StackId'] = event['StackId']
              response['RequestId'] = event['RequestId']
              response['LogicalResourceId'] = event['LogicalResourceId']

              if event['RequestType'] == 'Delete':
                  logger.info('Nothing to do. Request Type : {0}'.format(event['RequestType']))
                  response['Status'] = 'SUCCESS'
                  send_cfn_response(event, context, response['Status'], response)
              elif event['RequestType'] == 'Create' or event['RequestType'] == 'Update':
                  try:
                      # Log the environment variables we're passing to make debugging easier
                      wait_handle_url = event['ResourceProperties']['WaitHandleUrl']
                      logger.info(f"WaitHandleUrl being passed to CodeBuild: {wait_handle_url}")
                      
                      build = codebuild_client.start_build(
                          projectName=event['ResourceProperties']['CodebuildProjectName'],
                          environmentVariablesOverride=[
                              {
                                  'name': 'WAIT_HANDLE_URL',
                                  'value': wait_handle_url,
                                  'type': 'PLAINTEXT'
                              }
                          ]
                      )
                      logger.info(f"CodeBuild started with build ID: {build['build']['id']}")
                      response['Status'] = 'SUCCESS'
                      send_cfn_response(event, context, response['Status'], response)
                  except Exception as e:
                      logger.error('Error: {0}'.format(str(e)))
                      response['Status'] = 'FAILED'
                      response['Reason'] = str(e)
                      send_cfn_response(event, context, response['Status'], response)

          def send_cfn_response(event, context, response_status, response_data):
              response_body = json.dumps({
                  'Status': response_status,
                  'Reason': response_data.get('Reason', 'See the details in CloudWatch Log Stream: ' + context.log_stream_name),
                  'PhysicalResourceId': response_data.get('PhysicalResourceId', context.log_stream_name),
                  'StackId': event['StackId'],
                  'RequestId': event['RequestId'],
                  'LogicalResourceId': event['LogicalResourceId'],
                  'Data': response_data
              })

              headers = {
                  'content-type': '',
                  'content-length': str(len(response_body))
              }

              logger.info(f"Sending response to: {event['ResponseURL']}")
              
              req = urllib.request.Request(url=event['ResponseURL'], data=response_body.encode('utf-8'), headers=headers, method='PUT')

              try:
                  with urllib.request.urlopen(req) as response:
                      logger.info(f"Response sent. Status code: {response.getcode()}")
                      logger.info(f"Status message: {response.msg}")
              except Exception as e:
                  logger.error(f"Failed to send response: {str(e)}")
                
  CustomTriggerBuild:
    Type: Custom::TriggerBuild
    DependsOn: BuildProject
    Properties:
      ServiceToken: !GetAtt TriggerBuildLambda.Arn
      CodebuildProjectName: !Ref BuildProject
      WaitHandleUrl: !Ref EKSClusterWaitHandle

  EKSClusterWaitCondition:
    Type: AWS::CloudFormation::WaitCondition
    Properties:
      Handle: !Ref EKSClusterWaitHandle
      Timeout: '2700'  # 45 minutes
      Count: 1

  EKSClusterWaitHandle:
    Type: AWS::CloudFormation::WaitConditionHandle

Outputs:
  CoderURL:
    Description: Coder CloudFront URL
    Value: !Sub 'https://${CoderCloudFrontDistribution.DomainName}'

  CloudFrontDistributionId:
    Description: CloudFront Distribution ID
    Value: !Ref CoderCloudFrontDistribution

  CloudFrontDomainName:
    Description: CloudFront Distribution Domain Name
    Value: !GetAtt CoderCloudFrontDistribution.DomainName
  
  CoderAdminEmail:
    Description: Coder Admin Email
    Value: !Ref CoderAdminEmail

  CoderAdminPassword:
    Description: Coder Admin Password - retrieve from Secrets Manager using the ARN below
    Value: !Sub 'Stored in Secrets Manager: ${CoderAdminPasswordSecret}'

  CoderAdminPasswordSecretArn:
    Description: ARN of the Coder Admin Password Secret
    Value: !Ref CoderAdminPasswordSecret

  CoderSessionTokenSecretArn:
    Description: ARN of the Coder Session Token Secret
    Value: !Ref CoderSessionTokenSecret
  
  PostgreSQLConnectionURLWithoutPassword:
    Description: PostgreSQL Connection URL (without password)
    Value: !Sub 'postgresql://${Username}@${CoderAuroraCluster.Endpoint.Address}:${CoderAuroraCluster.Endpoint.Port}/${DatabaseName}'

  CloudFormationStack:
    Description: Stack name for reference in scripts
    Value: !Ref AWS::StackName

  FargateSubnet1Id:
    Description: Fargate Subnet 1 ID
    Value: !Ref FargateSubnet1

  FargateSubnet2Id:
    Description: Fargate Subnet 2 ID
    Value: !Ref FargateSubnet2

  FargatePodExecutionRoleArn:
    Description: Fargate Pod Execution Role ARN
    Value: !GetAtt FargatePodExecutionRole.Arn

  EfsFileSystemId:
    Description: EFS File System ID for workspace persistent storage
    Value: !Ref EfsFileSystem

  ClaudeCodeImageUri:
    Description: ECR URI for Claude Code workspace image
    Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-claude-code:latest'

  KiroCliImageUri:
    Description: ECR URI for Kiro CLI workspace image
    Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-kiro-cli:latest'

  ChallengeImageUri:
    Description: ECR URI for Challenge workspace image
    Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EKSClusterName}/coder-workspace-challenge:latest'

  BedrockMantleApiKeySecretArn:
    Description: ARN of the Bedrock Mantle API key secret (ServiceCredentialSecret)
    Value: !Ref BedrockMantleApiKeySecret

  BedrockApiKeyUserName:
    Description: IAM user that owns the Bedrock service-specific credential
    Value: !Ref BedrockApiKeyUser
